name: PyPI Publish (retired manual path)

on:
  workflow_dispatch:

permissions: {}

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Require the immutable release workflow
        run: |
          echo "::error::Direct manual PyPI publication is disabled. Create an immutable version tag from main and use the Release workflow, which reuses the tested artifact after all production gates."
          exit 1

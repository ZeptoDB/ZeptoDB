# 2026-03-23

## Session Start
- Main session with 진만 (first Kiro session)
- Read through entire APEX-DB project structure and documentation
- Project is a C++20 ultra-low latency in-memory database targeting kdb+ replacement

## Project Status Summary
- 467 C++ tests passing
- kdb+ replacement rate: HFT 95%, Quant 90%, Risk 95%
- Most core features complete; see BACKLOG.md for remaining items
- Latest commits: Kafka consumer design doc, CTE/subquery, SQL Phase 1-3, auth layer

## Session 2 — Multi-node work
- Reviewed full cluster codebase: QueryCoordinator, TcpRpc, PartialAgg, PartitionRouter
- **Connection pooling (Phase 2 RPC)** ✅ Completed
  - Server: keep-alive connections, SO_RCVTIMEO 1s, active fd tracking for clean shutdown
  - Client: fd pool (acquire/release), max 4 idle, stale fd detection via MSG_PEEK
- **WAL Replication** ✅ Completed
  - New RPC types: WAL_REPLICATE (batch of ticks) + WAL_ACK
  - `WalReplicator`: background thread, double-buffer queue, configurable batch_size/flush_interval
  - `TcpRpcClient::replicate_wal()`, `TcpRpcServer` WAL_REPLICATE handler
  - `serialize_wal_batch` / `deserialize_wal_batch` in rpc_protocol.h
  - Stats: enqueued/replicated/dropped/send_errors
  - 4 tests: RPC round-trip, serialization, end-to-end replicator, replica-down error counting
- **Partition Migration** ✅ Completed
  - `PartitionMigrator`: query source (SELECT * WHERE symbol=X) → WAL batch to dest
  - `migrate_symbol()` single symbol, `execute_plan()` for MigrationPlan
  - Handles empty symbols gracefully
  - 3 tests: single symbol, multi-symbol plan, empty symbol
- Fixed CMakeLists.txt: added `find_package(PkgConfig QUIET)` before Arrow section
- Total: 483 tests passing
- **Replication Factor 2** ✅ Completed
  - `PartitionRouter::route_replica()`: consistent hash ring에서 다음 distinct 물리 노드 반환
  - 3 tests
- **Auto Failover** ✅ Completed
  - `FailoverManager`: HealthMonitor DEAD → router + coordinator에서 제거, ring 자동 승격
  - `FailoverEvent` callback, `trigger_failover()` manual trigger
  - 3 tests: manual trigger, health monitor integration, replica-becomes-primary
- **Coordinator HA** ✅ Completed
  - `CoordinatorHA`: active-standby, standby pings active, auto-promote on failure
  - Standby forwards queries to active via RPC, promotes on timeout
  - `PromotionCallback` for caller to re-wire after promotion
  - 3 tests: active serves, standby promotes, standby forwards
- **Snapshot Coordinator** ✅ Completed
  - `SnapshotCoordinator`: parallel SNAPSHOT command to all nodes, collect results
  - `SnapshotResult`: per-node success/error, total_rows, snapshot_ts
  - 2 tests: two-node snapshot, partial failure
- Total: 490 tests passing
- **HA backlog fully complete!**
- **exec_simple_select parallelization** ✅ Completed
  - Partition-parallel SELECT: chunks → parallel scan → concat merge
  - Auto-fallback to serial for small data or single partition
  - Reuses ParallelScanExecutor infrastructure
- **Cross-node ASOF JOIN routing** ✅ Completed
  - QueryCoordinator detects ASOF/WINDOW JOIN → routes to symbol's node or scatter-gather
  - Symbol-filtered JOINs route to single node (Tier A-2)
  - No-filter JOINs scatter to all nodes, concat results
- **Distributed SELECT scatter-gather** — verified with SUM/COUNT/MIN/MAX across 2 nodes
- **CHUNKED mode** ✅ Completed
  - exec_agg_parallel + exec_simple_select_parallel: row range split for single large partitions
- **DistributedQueryScheduler** ✅ Completed
  - TCP RPC 기반 구현 (스텁 → 실제), QueryScheduler 인터페이스 준수
- **Data/Compute node separation** ✅ — ComputeNode, fetch_and_ingest, 2 tests
- **Resource isolation** ✅ — CPU pinning (pthread_setaffinity_np), IsolationConfig
- **Multi-threaded drain** ✅ — PipelineConfig::drain_threads, MPMC multi-consumer
- Total: 492 tests passing
- **Ring Buffer dynamic adjustment** ✅ — queue full → direct-to-storage bypass
- **Parquet reading** ✅ — ParquetReader::read_file/ingest_file
- **Tests added for all new components** — 20 new tests:
  - ChunkedMode: 6 tests (select_mode, make_row_chunks, agg parallel)
  - ParallelSelect: 2 tests (multi-partition concat, with limit)
  - ResourceIsolation: 5 tests (num_cpus, pin_to_core, empty set, range, config)
  - DistributedScheduler: 1 test (scatter/gather with RPC server)
  - MultiDrain: 2 tests (2 threads, 4 threads multi-symbol)
  - DirectToStorage: 1 test (queue full no data loss)
  - ParquetReader: 2 tests (nonexistent file error, ingest returns zero)
  - ComputeNode: already had 2 tests
- **README.md updated**: architecture diagram, project structure, test count badge (511+)
- Total: 511 tests passing

## Session 3 — SQL/Query completeness (Kiro)

### FULL OUTER JOIN ✅
- `FULL [OUTER] JOIN` token + parser + AST (`JoinClause::Type::FULL`)
- `HashJoinOperator::execute()`: FULL mode — LEFT JOIN + `right_matched[]` tracking for unmatched right rows
- `exec_hash_join`: FULL dispatch in both symbol-filtered and non-filtered paths
- 4 tests

### SUBSTR ✅
- `SUBSTR(col, start[, len])` — 1-based, optional length
- Tokenizer: `SUBSTR` keyword → `TokenType::SUBSTR`
- Parser: `parse_arith_primary()` — 2-3 arg function, `func_arg2` for length
- `ArithExpr::func_arg2` field added to AST
- `eval_arith()` + `eval_arith_vt()`: int64 → string → substr → stoll
- 4 tests

### UNION JOIN (kdb+ uj) ✅
- `UNION JOIN table` — ON clause optional
- Parser: `is_union_join = check(UNION) && peek() == JOIN` to avoid conflict with `UNION ALL`
- `exec_union_join`: union of column names, JOIN_NULL for missing, concatenate all rows
- 3 tests

### PLUS JOIN (kdb+ pj) ✅
- `PLUS JOIN table ON key = key`
- `TokenType::PLUS_JOIN` (renamed from PLUS to avoid conflict with arithmetic +)
- `exec_plus_join`: hash lookup by key, overlapping numeric columns added
- 3 tests

### AJ0 (left-columns-only ASOF JOIN) ✅
- `AJ0 JOIN table ON ...`
- `JoinClause::Type::AJ0`, reuses `exec_asof_join` with `aj0_mode` flag
- Right-alias columns skipped in column_names and row assembly
- 3 tests

### Summary
- All 5 SQL/Query backlog items completed
- 565 tests passing (was 548, +17 new)
- Files modified: tokenizer.h, tokenizer.cpp, ast.h, parser.cpp, executor.h, executor.cpp, join_operator.h, join_operator.cpp, test_sql.cpp, BACKLOG.md
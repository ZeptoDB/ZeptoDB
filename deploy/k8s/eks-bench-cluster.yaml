# k8s/eks-bench-cluster.yaml — EKS Auto Mode cluster for ZeptoDB
# Usage: eksctl create cluster -f deploy/k8s/eks-bench-cluster.yaml
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: zepto-bench
  region: ap-northeast-2
  version: "1.35"

autoModeConfig:
  enabled: true
